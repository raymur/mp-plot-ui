{
  "name": "mp-plot",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "postinstall": "npm install --prefix mp-plot-ui",
    "build": "cd mp-plot-ui && npm run build && rm -rf node_modules",
    "start": "serve -s mp-plot-ui/build",
    "serve": "serve -s mp-plot-ui/build"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
